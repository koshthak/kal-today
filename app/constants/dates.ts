export type datesConstTypes = {
  DAYS_IN_WEEK: number;
  TOTAL_MONTH: number;
  MAX_ROW_IN_MONTH: number;
  MIN_ROW_IN_MONTH: number;
};

const DATES_CONST: datesConstTypes = {
  DAYS_IN_WEEK: 7,
  TOTAL_MONTH: 12,
  MAX_ROW_IN_MONTH: 6,
  MIN_ROW_IN_MONTH: 5
};

export default DATES_CONST;
