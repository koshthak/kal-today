export type RouteTypes = {
  HOME: string;
  MONTHLY: string;
  WEEKLY: string;
  DAILY: string;
};

const ROUTES: RouteTypes = {
  HOME: '/',
  MONTHLY: '/monthly',
  WEEKLY: '/weekly',
  DAILY: '/daily',
};

export default ROUTES;
