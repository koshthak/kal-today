import React, { useState } from 'react';
// import { DateRangePicker } from 'react-date-range';
// import { addDays } from 'date-fns';
// import moment from 'moment';

import styles from './datepicker.scss';

// type Props = {
//   setRange: React.SetStateAction<unknown>,
//   conatinerClassName?: string,
//   showSelectionPreview?: boolean,
// };

const DatePickerSelectionPreview = () => {
  return (
    <div className={styles['slection-preview']}>slection preview ndskj</div>
  );
};

// DatePickerSelectionPreview.defaultProps = {
//   conatinerClassName: '',
//   showSelectionPreview: false,
// };

export default DatePickerSelectionPreview;
